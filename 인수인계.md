# 업무 스케줄러 — 인수인계 / 작업 핸드오버

> 이 문서는 다른 PC(또는 새 대화)에서 Claude에게 이 프로젝트를 이어서 작업하도록 맥락을 전달하기 위한 인수인계 파일입니다.
> 새 대화를 시작할 때 **이 파일과 `scheduler.html`을 함께 첨부/언급**하면 됩니다.

---

## 0. 한 줄 요약

**망분리(오프라인) 환경의 iPad에서 단독 실행되는, 외부 의존성 0인 단일 HTML 업무 스케줄러.**
타겟일에서 **영업일 기준 역산**으로 최소 시작일을 계산하고, 스케줄 최적화 제안·캘린더·진행 체크·PDF/Excel/CSV/JSON 출력을 제공한다.

---

## 1. 프로젝트 개요 / 제약 조건

- **파일**: `scheduler.html` (이 문서와 같은 폴더, 약 1,480줄 / 70KB, 단일 파일)
- **실행 환경**: 망분리된 외부망, **iPad Safari 단독 사용** (사용자 혼자 사용)
- **핵심 제약**: 인터넷·CDN·서버·외부 라이브러리 **전부 사용 불가**
  - → 모든 HTML/CSS/JS를 한 파일에 인라인
  - → 폰트는 애플/윈도우 시스템 폰트, 아이콘은 인라인 SVG·이모지
  - → 데이터는 브라우저 `localStorage`에 저장 (서버 없음)
  - → **Excel(.xlsx)도 라이브러리 없이 순수 JS로 직접 생성** (ZIP/CRC32 구현)
- **목적**: 데이터를 누적해 나중에 연간 업무 양식으로 활용 → PDF/Excel 출력 필수
- **디자인**: 사용자 친화적·미래지향·**다크모드**, 중요도/긴급도에 따른 가시성 강조

---

## 2. 파일 열기 / 작업 방법

### 사용자(실제 사용)
- `scheduler.html`을 iPad/PC 브라우저에서 더블클릭하면 바로 실행. 서버 불필요.

### Claude(개발·검증) 주의사항
- `file://` 직접 접근은 Playwright/브라우저 도구에서 차단됨.
  → **임시 정적 서버**를 띄워서 확인한다 (작업 후 반드시 종료):
  ```bash
  node -e 'const http=require("http"),fs=require("fs");http.createServer((q,s)=>{const f=q.url==="/"?"/scheduler.html":q.url;try{const b=fs.readFileSync("."+f);s.writeHead(200,{"Content-Type":"text/html; charset=utf-8"});s.end(b);}catch(e){s.writeHead(404);s.end();}}).listen(8731,()=>console.log("up"));'
  ```
  그 후 `http://localhost:8731/` 로 접속해 스크린샷/조작 검증.
- **JS 문법 검사**(브라우저 없이 빠르게):
  ```bash
  node -e 'const fs=require("fs");const m=fs.readFileSync("scheduler.html","utf8").match(/<script>([\s\S]*?)<\/script>/);fs.writeFileSync("_c.js",m[1]);' && node --check _c.js && echo OK && rm -f _c.js
  ```
- **로직 단위 테스트**: `<script>` 본문을 추출 → `document`/`localStorage`/`TextEncoder`를 스텁 → `eval` 후 `computeSchedule`/`optimize` 등 호출.
  - 주의: `eval` 안의 `let tasks`는 eval 스코프에 갇히므로, 테스트 코드를 `code += "..."`로 **eval 내부에 붙여** 같은 스코프에서 실행해야 한다.

---

## 3. 데이터 모델 (localStorage)

- 저장 키: **`work_scheduler_v1`** (업무 배열)
- 임시저장(작성 중 보호) 키: **`work_scheduler_draft_v1`**

```js
task = {
  id, title, owner, tag, note,
  importance: 1~4,            // 1 낮음 / 2 보통 / 3 높음 / 4 긴급
  target: "YYYY-MM-DD",       // 타겟 완료일
  _open: bool,                // UI 펼침 상태(저장 시 JSON 백업에서는 제거)
  stages: [                   // 단계 = 화면상 "세부업무"
    {
      name, dur,              // dur = 소요 '영업일'(주말 제외)
      phase: "before"|"on"|"after",  // 타겟 이전 / 타겟 당일 / 타겟 이후
      done: bool,             // 세부내용 없는 단계의 완료 플래그(있으면 자동 파생)
      details: [ { text, done } ]    // 세부내용(체크 항목)
    }
  ]
}
```

용어 정리(중요): 화면 표기 기준
- **업무** = task
- **세부업무** = stage (단계)
- **세부내용** = detail (stage 하위 체크 항목)

---

## 4. 구현된 기능 전체 목록 (시간순으로 누적된 요구사항 반영 결과)

### 4.1 기본
- 업무 추가/수정/삭제, 진행중/완료/지연·긴급/전체 필터, 긴급도·타겟일·중요도·진행률 정렬, 검색
- 통계 대시보드(진행중/지연/긴급/완료/평균 진행률)
- 카드 UI: 중요도 뱃지, D-Day, 진행률 바, 긴급도별 좌측 테두리 색 + 지연 시 빨강 펄스 글로우

### 4.2 스케줄 계산 — **영업일(주말 제외) 기준**
함수: `computeSchedule(task)` (약 674줄~)
- **타겟 이전(before)**: 타겟 직전 영업일까지 끝나도록 **역산** → 각 단계의 "최소 시작일"
- **타겟 당일(on)**: 타겟일에 수행(예: 회의). 타겟일부터 순행
- **타겟 이후(after)**: 당일 업무 종료 다음 영업일부터 순행(예: 결과 보고)
- 모든 `dur`는 영업일. 주말은 자동 제외.
- 반환: `taskStart`(최소 시작일), `taskEnd`(완료 예정일), `stageInfo[]`(단계별 start/end/phase), `progress`, `allDone`, `slack`(시작까지 남은 영업일), `level`(overdue/urgent/soon/ok/done)
- 영업일 헬퍼: `isBiz / nextBiz / prevBiz / addBizFwd / addBizBack / bizDelta` (약 654줄~)

### 4.3 스케줄 최적화 엔진 — `optimize()` (약 1080줄~)
단순 역산을 넘어 "최적화"를 제공:
- 🔀 **병행 가능**: 진행 기간이 겹치는 업무 묶음 추천
- ⚠️ **업무 집중(부하)**: 특정 기간 작업량 몰림 경고 + 여유 적은 업무부터 조기 착수 권장(영업일 기준)
- 🚀 **즉시 착수**: 최소 시작일 지난 업무
- ⏰ **마감 위험**: 혼자 직렬 처리 가정 EDD 시뮬레이션으로 지연 예측(영업일)
- 📦 **묶음 처리**: 같은 분류(태그) 업무
- 대시보드 상단 접이식 패널에 표시

### 4.4 캘린더 뷰
- 상단 `📋 리스트 / 📅 캘린더` 토글 (`setView`)
- 월 그리드: 단계 진행 구간을 긴급도 색 칩으로, 타겟일은 🎯, 주말 칸은 비움(영업일)
- **일자 클릭 → 상세 팝업**(`showDay`): 그날 업무·단계·담당자·기간 + 세부내용 체크리스트(팝업에서 바로 체크), 수정 버튼
- 칩 색상: 긴급도 + phase(당일=레드, 이후=보라 점선)

### 4.5 단계/세부내용 체크 (계층형 진행 관리)
- **세부내용** 체크박스: `toggleDetail`
- **단계(세부업무)** 체크박스: `toggleStage`
  - 하위 세부내용이 있으면 → 클릭 시 **전체 체크/해제**, 전부 체크되면 **자동 완료(파생)**
  - 세부내용이 없으면 → 단계 자체 `done` 플래그 토글
- 진행률 = 단위 합계(세부내용이 있으면 각 항목 1단위, 없으면 단계 1단위)
- 캘린더 팝업에도 동일 체크박스(`toggleStageCal`/`toggleDetailCal`)

### 4.6 타겟 시점 3단계 + 가시성
- 입력 모달의 단계별 시점 토글: **🎯 타겟 이전 → 📍 타겟 당일 → 🏁 타겟 이후** 순환(`onPhase`)
- 색상 구분: **타겟 이전 = 청록 / 타겟 당일 = 레드(강조) / 타겟 이후 = 보라**
- **타겟 당일**은 레드 박스 + 글로우 + "● 당일 수행 · 핵심 마일스톤" 플래그 + 채워진 레드 배지로 강조

### 4.7 입력 폼 (상태 기반)
- `formState` 단일 상태 + 입력 즉시 `localStorage` 임시저장 → 모달 닫거나 다른 화면 만져도 **작성 내용 보존**(복원 알림)
- 단계 **중간 삽입**(`insertStage`) + 순서 이동(`moveStage` ▲▼) + 삭제
- 바깥 영역 탭으로 닫히지 않음(실수 방지), "초기화"로만 명시적 삭제

### 4.8 출력 / 백업
- **PDF**: `window.print()` + 인쇄 전용 스타일(캘린더 숨기고 항상 리스트 인쇄)
- **Excel(.xlsx)**: 라이브러리 없이 순수 JS ZIP 생성 — `crc32`/`buildZip`/`exportXLSX` (약 1170줄~). `unzip -t` 검증 완료
- **CSV**: UTF-8 BOM(한글 호환)
- **JSON 백업/복원**: `backupJSON`/`restoreJSON` — 망분리라 서버 백업 없음 → **주기적 JSON 백업 권장**
- 출력 표(`buildRows`)에 단계 시점(타겟 이전/당일/이후)·완료여부·완료예정일 포함

### 4.9 iPad 대응
- 애플 시스템 폰트 우선, `-webkit-backdrop-filter`, 터치 타깃 확대, 자동확대 방지, 탭 하이라이트 제거
- 파일 다운로드(PDF/Excel/CSV/JSON)는 iPadOS Safari에서 **파일 앱**으로 저장됨

---

## 5. 코드 구조 / 주요 함수 위치 (대략적 라인, 수정 시 grep으로 재확인)

- CSS: `<style>` 상단 ~약 460줄 (변수는 `:root`)
- HTML 본문: 헤더/툴바, 통계, 컨트롤(뷰토글·검색·필터), 최적화 패널, `#tasks`, `#calendar`, 일자 팝업 `#dayOverlay`, 입력 모달 `#overlay`, 토스트
- JS:
  - 날짜/영업일 유틸, `computeSchedule` (≈600~720)
  - `render` / `cardHTML` / `stageRow` / `ddayText` / 통계 (≈740~900)
  - 상호작용: `toggleOpen/toggleDetail/toggleStage/delTask` (≈905~)
  - 모달/폼(상태기반): `formState`, `openModal/editTask/saveTask/renderStages/onPhase` 등 (≈940~1075)
  - 최적화: `optimize/renderOptimize` (≈1078~)
  - 캘린더: `setView/renderCalendar/itemsOnDay/showDay/toggleDetailCal/toggleStageCal` (≈1140~1310)
  - 출력: `buildRows/exportCSV/crc32/buildZip/exportXLSX/backupJSON/restoreJSON` (≈1320~)
  - 초기화: `initCal(); render();` (파일 끝)

---

## 6. 설계 결정 / 왜 이렇게 했나 (재논의 방지용)

- **단일 HTML / 의존성 0**: 망분리 필수 조건. 새 기능도 이 원칙 유지.
- **localStorage 저장**: 서버 불가. 단, Safari 데이터 삭제 시 유실 → JSON 백업으로 보완.
- **순수 JS XLSX**: SheetJS 등 사용 불가. stored(무압축) ZIP + inlineStr 방식. 새 시트/스타일 필요하면 이 구조 확장.
- **영업일 = 주말만 제외**: 공휴일(설·추석 등)은 **미반영**. 필요 시 공휴일 목록 입력 기능 추가 논의(매년 수동 입력 방식이 될 것).
- **타겟 당일 = 레드**: 이전엔 당일/이전이 같은 청록이라 구분이 안 됐음 → 사용자가 레드로 변경 요청, 강조까지 적용.
- **D-Day/slack은 영업일 기준**: 주말 제외가 업무 계획에 더 정확.

---

## 7. 미해결 / 향후 아이디어 (백로그)

- 공휴일 반영(영업일 계산에서 제외) — 현재 주말만 제외
- 반복 업무 템플릿(매월/매분기 복제)
- 간트 타임라인 뷰(막대형)
- 캘린더 주간(week) 뷰
- 색 톤/플래그 문구 등 미세 조정 여지

---

## 8. 다음 대화에서 작업 이어가는 법

1. 새 대화에 `scheduler.html` 과 이 `인수인계.md` 를 함께 제공.
2. "이 파일 이어서 작업할 거야"라고 말하고, 원하는 변경 요청.
3. Claude는 수정 후 **① `node --check`로 문법 검사 → ② 로직 단위 테스트 → ③ 임시 서버+스크린샷으로 시각 검증 → ④ 임시 파일/서버 정리**의 순서로 진행하면 됨(2장 참고).

---

_최종 갱신: 2026-06-09 · 작성: Claude (Claude Code)_
